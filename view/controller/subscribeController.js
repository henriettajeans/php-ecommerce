/** @format */

function subscribe(plan) {
  // Handle subscription process here, such as calling a PHP script to create a subscription via Stripe API
}

function selectStart(id) {}
function selectMedium(id) {}
function selectAdvanced(id) {}
